# ============================================================================
# STOXX50 Equity Research Platform — Python dependencies
# Target runtime: Python 3.12
# ============================================================================

# --- Core: dashboard, data, computation ------------------------------------
dash==2.18.2
dash-bootstrap-components==1.6.0
plotly==5.24.1
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1                # Black-Scholes (norm), IV solver (brentq), surface (griddata)

# --- Market data ------------------------------------------------------------
yfinance>=1.5.1             # Yahoo Finance: prices, fundamentals, option spot
                            # IMPORTANT: 0.2.x is broken (Yahoo crumb/cookie change).
                            # 1.x uses curl_cffi and fixes "Expecting value: line 1 column 1".
curl_cffi>=0.7.0           # required by yfinance 1.x for Yahoo session handling
requests==2.32.3            # Groq/Grok LLM API calls

# --- Statistics (Math Lab: cointegration, ADF) -----------------------------
statsmodels==0.14.4

# --- Reporting --------------------------------------------------------------
openpyxl==3.1.5              # Excel track-record export

# ============================================================================
# Optional — only needed for live IBKR paper trading (--mode live)
# Install with: pip install ib_insync nest_asyncio
# ============================================================================
ib_insync==0.9.86            # Interactive Brokers TWS API
nest_asyncio==1.6.0          # Allows ib_insync event loop inside Dash/Flask threads

# ============================================================================
# Notes
# ----------------------------------------------------------------------------
# - On managed environments (Debian/Ubuntu): add --break-system-packages
#   e.g. pip install -r requirements.txt --break-system-packages
# - AI Lab: set GROQ_API_KEY=gsk_... in .env (free at console.groq.com)
# - The platform runs fully without IBKR (paper trading is simulated in Python).
# ============================================================================